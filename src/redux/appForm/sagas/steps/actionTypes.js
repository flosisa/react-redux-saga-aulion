import { STEPS } from '../../actionTypes';

const STEPS_REQUEST = `${STEPS}_REQUEST`
const STEPS_SUCCESS = `${STEPS}_SUCCESS`
const STEPS_ERROR = `${STEPS}_ERROR`

export {
  STEPS_REQUEST,
  STEPS_SUCCESS,
  STEPS_ERROR
}