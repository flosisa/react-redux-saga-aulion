import { LOGOUT } from '../../actionTypes';

const LOGOUT_REQUEST = `${LOGOUT}_REQUEST`
const LOGOUT_SUCCESS = `${LOGOUT}_SUCCESS`
const LOGOUT_ERROR = `${LOGOUT}_ERROR`

export {
  LOGOUT_REQUEST,
  LOGOUT_SUCCESS,
  LOGOUT_ERROR
}