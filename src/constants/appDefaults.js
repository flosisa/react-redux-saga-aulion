import { en } from './localeOptions'

const defaultProtocol = 'https://'
const host = 'localhost'
const port = 7111
const apiServer = 'api.domain.com'
const authServer = 'auth.domain.com'
export const origin = `${defaultProtocol}${host}:${port}`
export const apiServerOrigin = `${defaultProtocol}${apiServer}`
export const authServerOrigin = `${defaultProtocol}${authServer}`

export const adminEmail = 'newsuzbek@gmail.com'
export const defaultLocale = en
