export const API_URL = '/api';

// menu
export const DISTRICTS_BY_REGION = `${API_URL}/get-districts-by-region`
export const REGIONS = `${API_URL}/get-regions`
export const ENTITY_TYPES = `${API_URL}/get-entity-types`
export const SPECIALTIES = `${API_URL}/get-specialties`

// profile
const PROFILE = `${API_URL}/profile`
export const SIGN_UP = `${PROFILE}/signUp`
export const SIGN_IN = `${PROFILE}/signIn`

// ***Secure endpoints

// profile
export const ACCOUNT = `${PROFILE}/account`
